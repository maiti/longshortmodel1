# Local development extras on top of the core runtime deps in
# requirements.txt (the file Vercel's build actually installs for the
# deployed function -- see api/requirements.txt and README's "Deploying
# to Vercel" section). requirements.txt now includes yfinance and
# pyarrow too (real data and its cache work in production, not just
# locally); what's left here is purely local-only tooling: matplotlib
# and uvicorn aren't needed by the deployed function at all, and pytest/
# httpx are dev/test tools. A previous version of requirements.txt also
# carried a scipy dependency, since eliminated entirely (see
# quant/validation.py) -- that one was never a deliberate inclusion, it
# was pandas' internal dependency for one method call, discovered the
# hard way when removing it broke production.
#
# Install everything needed for local development with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

uvicorn[standard]>=0.29   # run the local dev server (webapp/server.py)
matplotlib>=3.8            # scripts/run_model.py's equity_curve.png
pytest>=8.0
httpx>=0.27                 # FastAPI test client, used by future API tests
